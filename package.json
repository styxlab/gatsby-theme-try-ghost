{
  "name": "gatsby-theme-try-ghost-workspace",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "develop": "yarn workspace try-ghost develop",
    "build": "yarn workspace try-ghost build",
    "clean": "yarn workspace try-ghost clean"
  },
  "workspaces": [
    "gatsby-theme-try-ghost",
    "try-ghost"
  ]
}
